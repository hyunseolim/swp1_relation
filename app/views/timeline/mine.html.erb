<h1><%=@me.email%>의 타임라인</h1>
<%=form_tag '/write', method: 'post'%>
    <textarea class="form-control" rows="3" name="naeyong"></textarea>
    <input class="btn btn-success" type="submit">
</form>



<ul>
    <% @blogs.each do |b| %>
        <li>
            <p class = "bg-info"><%=b.user.email%> :</p>
            <div class="well">
                <%=b.content%>
            </div>
            <%=form_tag '/comment', method: 'post', class: "form-inline" %>
                <input type="hidden" name="blog_id" value="<%=b.id%>">
                <div class="form-group">
                    <input type="text" name="comment" class="form-control">
                </div>
                <div class="form-group">
                    <input type="submit" class="btn btn-primary">
                </div>
            </form>
            <ol>
                <% b.comments.each do |c| %>
                 <li><p class = "bg-danger"><%=c.user.email%> : <%=c.msg%><p></p></li>
            <% end %>
            </ol>
        </li>
    <% end %>
</ul>